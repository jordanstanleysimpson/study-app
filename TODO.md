# TODO

- [ ] Show correct meaning on wrong multiple choice answer — display a feedback line with the word's meaning when an incorrect choice is picked
- [ ] Widen class/unit picker dropdowns — increase width so text isn't truncated
- [ ] Collapse Spanish units into a single "All" unit — merge the 4 separate units into one combined list
- [ ] Clear choice highlight on auto-advance — previous correct answer button stays highlighted when next card loads; needs to be cleared before rendering new choices
